# 环境变量
```
cat /etc/shells
```
## 环境变量文件
```
/etc/profile
/etc/paths
~/.bash_profile
~/.bash_login
~/.profile
~/.bashrc
```
```
~/.zshrc
```
## 设置环境变量
```
touch ~/.bash_profile
vim ~/.bash_profile
export PATH=$PATH:/haha/path
source ~/.bash_profile
```

## 设置Go2Shell的默认Shell
要打开Go2Shell的Preference页面，在shell里面敲
open -a Go2Shell --args config

